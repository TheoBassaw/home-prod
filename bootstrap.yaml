---
- name: Create Home Cluster
  become: false
  hosts: localhost
  tasks:
    - name: Creating Talos Cluster
      community.general.terraform:
        project_path: "{{ playbook_dir }}/terraform/talos/cluster"

    - name: Applying initial helm and manifests
      community.general.terraform:
        project_path: "{{ playbook_dir }}/terraform/talos/manifests"