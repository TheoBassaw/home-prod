apiVersion: v1
kind: ConfigMap
metadata:
  name: helm-values
  namespace: external-secrets
data:
  helm-values.yaml: |
    installCRDs: true
    resources:
      requests:
        cpu: 10m
        memory: 32Mi